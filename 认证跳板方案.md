# pm-assist 认证跳板（Auth Hub）方案

> 目标：让同级目录下的其他应用（chatlogger、meeting-recorder 等）复用 pm-assist 这个飞书企业应用的凭证与权限完成认证，**不再**为每个新应用去飞书开放平台建应用、申请权限、配可用范围。

## 1. 背景与现状

### 1.1 现状盘点

| | pm-assist | chatlogger |
|---|---|---|
| 飞书应用 | 自建企业应用，已配全面数据读取权限 | 另建了一个自建应用 |
| token 类型 | `tenant_access_token`（机器人发消息）+ OAuth v1（admin 登录） | OAuth v2 `user_access_token`（以用户身份读消息/文档/多维表格） |
| 所需 scope | contact:user.base:readonly 等 | im:message:readonly、bitable:app、docx/wiki、媒体下载等 8 项 |
| 部署 | pm.tmhcorps.cn（FastAPI + uvicorn 单进程 + Cloudflare） | chatlogger.tmhcorps.cn（Flask） |

### 1.2 痛点

每开发一个新应用，都要在飞书开放平台重复：建应用 → 逐项申请权限（等审批）→ 配置可用范围 → 配置重定向 URL。审批链路长，且权限本质上是重复申请。

### 1.3 关键事实（决定方案可行性）

- `user_access_token` / `tenant_access_token` 本身就是调用凭证：**谁持有 token，谁就能调对应 API**，飞书不校验"调用方服务器是不是创建 token 的那台"。
- 因此只要由 pm-assist 用自己的 APP_ID/SECRET 统一完成 OAuth 换发，再把 token 交给兄弟应用，兄弟应用即可直连飞书 OpenAPI。
- 飞书一个应用最多可配 300 个重定向 URL（[官方文档](https://open.feishu.cn/document/uYjL24iN/uYjN3QjL2YzN04iN2cDN?lang=zh-CN)），跳板只需新增 1 个（`/hub/callback`），不影响现有 `/admin/oauth/callback`。

## 2. 方案选型

| 方案 | 做法 | 结论 |
|---|---|---|
| A. 直接下发 APP_SECRET | 各应用 .env 里都放 pm-assist 的 APP_ID/SECRET，各自走 OAuth | ❌ secret 散落多台服务器，泄露面大；无审计、无法单独吊销某个应用；各应用重复请求 tenant token 浪费配额 |
| **B. 认证跳板（推荐）** | pm-assist 内嵌 hub 模块：OAuth 换发、token 刷新、tenant token 下发全部收口，secret 不出 pm-assist | ✅ 集中管控 + 审计，接入方改动小 |
| C. 全量 API 代理 | 兄弟应用所有飞书调用都经 pm-assist 转发 | ❌ 工作量大、延迟高、pm-assist 成为全流量瓶颈，对 chatlogger 高频同步场景不划算 |

选 B：兄弟应用只在「登录、回调、刷新」三处改走 hub，拿到 token 后**依旧直连飞书**，业务代码零改动。

## 3. 总体设计

### 3.1 授权流程（以 chatlogger 为例）

```
浏览器                chatlogger            pm-assist Hub           飞书开放平台
   │ ①点击「飞书登录」      │                      │                      │
   │────（登录按钮直链）─────────────────────────▶│                      │
   │                      │                 ② 302 授权页                │
   │◀────────────────────────────────────── ───── ───────────────────▶ │
   │                 ③ 用户确认，回调 code=xxx         │                  │
   │◀────────────────────────────────────── ───── ──────────────────── │
   │                      │              ④ code 换 user_access_token    │
   │                      │                      │ ───────────────────▶│
   │                      │  ⑤ 302 回 chatlogger + 一次性 auth_code      │
   │◀─────────────────────┼────────────────────── │                     │
   │                      │  ⑥ POST /hub/api/token（client_secret 换真 token）
   │                      │ ───────────────────▶ │                     │
   │                      │  ⑦ 持 user_access_token 直连 OpenAPI         │
   │                      │ ───────────────────────────────────────────▶│
```

- ①–⑤ 走浏览器 302；⑥ 起是服务端对服务端。
- refresh_token 到期刷新同理：chatlogger 调 `POST /hub/api/refresh`，由 hub 用 pm-assist 凭证完成刷新后返回新 token 对。
- 需要 app 级调用的应用（如发消息机器人）走 `POST /hub/api/tenant-token`，hub 返回集中缓存的 `tenant_access_token`。

### 3.2 pm-assist 侧新增：`auth_hub.py` + `hub_main.py`（同仓库、独立进程）

| 端点 | 方法 | 说明 |
|---|---|---|
| `/hub/authorize?client_id=...&state=...` | GET | 校验 client 注册信息 → 生成 HMAC 签名 state → 302 飞书授权页（pm-assist 的 APP_ID + 全局授权 scope） |
| `/hub/callback?code=...&state=...` | GET | 校验 state → v2 接口换 user_access_token → 生成一次性 auth_code（5 分钟、单次）→ 302 回 client 的 redirect_uri |
| `/hub/api/token` | POST | client_id + client_secret + 一次性 auth_code → 返回 access_token / refresh_token / open_id / name |
| `/hub/api/refresh` | POST | client_id + client_secret + refresh_token → 返回新 token 对 |
| `/hub/api/tenant-token` | POST | client_id + client_secret → 返回缓存的 tenant_access_token（复用 feishu.py 的缓存，全内网只向飞书请求一份） |

实现要点：

- **一次性 auth_code 存内存 dict**（TTL 5 分钟，用后即删）。当前 uvicorn 单进程部署，无需落库。
- **配置热加载**：`hub_config.json`（全局 scope + client 注册表）每次请求现读，改 scope、注册新应用均免重启。
- **state 签名**：`HMAC(SESSION_SECRET, client_id + nonce + ts)`，无状态、防伪造防 CSRF，10 分钟有效。
- 换 token 用 chatlogger 已验证过的 v2 接口（`/authen/v2/oauth/token`），自带 refresh_token 与 `offline_access`，与 chatlogger 现有 token 生命周期管理一致。
- 审计：每次签发/刷新写 logs/hub.log（client、open_id、时间、结果）。

部署形态（同仓库、独立进程，业务发版不连坐）：

- `hub_main.py`：仅挂载 hub 路由的入口，uvicorn 跑 127.0.0.1:8001
- `deploy/pm-hub.service`：第二个 systemd unit；业务发版只重启 pm-assist.service，hub 不动
- nginx 增加 `location /hub/ { proxy_pass http://127.0.0.1:8001; }`，对外 URL 恒为 `pm.tmhcorps.cn/hub/...`，飞书侧回调配置永不变化
- pm-assist 自身 admin OAuth（`/admin/oauth/callback`）保持原样，与 hub 并存

### 3.3 Hub 配置：`hub_config.json`（热加载，改动免重启）

```json
{
  "scopes": ["im:message", "bitable:app", "..."],
  "clients": [
    {"id": "chatlogger", "secret": "xxx",
     "redirect_uri": "https://chatlogger.tmhcorps.cn/auth/callback"},
    {"id": "meeting-recorder", "secret": "yyy", "redirect_uri": "https://..."}
  ]
}
```

- **scope 全局一份**：飞书用户授权是累积的（最新 token 包含用户历史上授予的全部权限），跨 client 拆分 scope 本就是软隔离；统一申请后，接入应用需要 pm-assist **已开通**的新权限时 hub 零改动，存量 token 也无需重新授权。
- **scopes 必须与飞书后台已开通的用户授权权限严格一致**：混入未开通项会导致授权页报 20027、打挂所有登录。飞书单次授权上限 200 个，故从后台导出后需裁剪：① 平台内部/AI 类（aily、spark、document_ai、collab_plugins 等）② 被同资源写权限包含的只读项（如保留 `im:message` 裁掉 `im:message:readonly`，飞书 API 二选一即可）③ 敏感 HR 字段（离职信息、工位、分机号等）。
- **注册新应用 = clients 加一项**，无需重启。secret 是接入方与 hub 之间的凭证，与飞书无关。
- 文件含密钥不入库（已 gitignore），模板见 `hub_config.json.example`。

### 3.4 chatlogger 侧改造（共 4 处，业务代码不动）

1. `config.py`：删 `FEISHU_APP_ID/SECRET`，加 `HUB_URL / HUB_CLIENT_ID / HUB_CLIENT_SECRET`。
2. `FeishuClient.get_authorize_url()`：改为返回 `https://pm.tmhcorps.cn/hub/authorize?client_id=...&state=...`。
3. `/auth/callback`：code 换 token 从「直接调飞书」改为「调 `POST /hub/api/token`」。
4. `FeishuClient._refresh_access_token()`：改为「调 `POST /hub/api/refresh`」（保留现有全局刷新锁逻辑）。

之后所有 `_api_get/_api_post` 直连飞书的业务代码完全不变。

## 4. 飞书后台一次性配置清单

现有配置**均不修改**：事件订阅回调 `https://pm.tmhcorps.cn/webhook/feishu` 与重定向 URL `https://pm.tmhcorps.cn/admin/oauth/callback` 保持原样（hub 与它们无关）。

需要做的只有：

1. pm-assist 应用 → 安全设置 → 重定向 URL 列表**新增一条**（不是修改）：`https://pm.tmhcorps.cn/hub/callback`（一个应用最多 300 个，多条并存）。
2. 权限核对：把 chatlogger 的 8 个 scope 在 pm-assist 应用上逐一确认已开通（用户授权型权限）；缺的这一次补齐——**这是最后一次审批**，此后所有新应用复用。
3. 可用范围确认：覆盖所有潜在使用者（跳板模式下用户授权记录在 pm-assist 应用名下）。

## 5. 实施步骤

| 阶段 | 内容 | 产出 |
|---|---|---|
| P1 | pm-assist 新增 `auth_hub.py` / `hub_main.py` / `deploy/pm-hub.service`，nginx 加 `/hub/` 转发；飞书后台一次性配置（第 4 节） | 跳板服务上线，可用 curl 验证 token 下发 |
| P2 | chatlogger 按 3.4 改造，走一遍完整授权 + 群同步回归 | 第一个接入应用，原飞书应用可停用 |
| P3（可选） | 事件分发：pm-assist `/webhook/feishu` 按 client 订阅把事件 POST 到各应用回调 | 需要事件推送的应用（如 meeting-recorder）也能复用 |
| P4（按需） | 其他应用（ones-helper、OuyiAgent…）逐个接入，每个接入成本 ≈ hub_config.json 加一项注册（免重启）+ 应用侧改 4 处 | 全家桶统一认证 |

pm-assist 自身的 admin OAuth（`/admin/oauth/callback`）保持原样，与 hub 并存，无需迁移。

## 6. 风险与权衡

| 风险 | 说明 | 缓解 |
|---|---|---|
| 秘钥集中 | pm-assist 的 APP_SECRET 成为最高价值目标，泄露影响面=全部应用 | secret 只存 pm-assist 一处；hub API 强制 client 凭证；审计日志；必要时加 IP 白名单 |
| 单点 | pm-assist 宕机 → 新授权与刷新中断 | 已有 token 有效期内不受影响；refresh_token 30 天内可等恢复；systemd Restart=always 已具备 |
| 授权连带 | 用户在飞书撤销 pm-assist 应用授权 → 所有接入应用同时失效 | 文档告知；各应用捕获 401 引导重新授权（chatlogger 已有此逻辑） |
| 配额共享 | 全部应用共享 pm-assist 应用的 API QPS 配额 | 内部工具量级可接受；tenant token 集中缓存本身就在省配额 |
| 审计归并 | 飞书侧所有调用记在 pm-assist 应用名下 | hub 审计日志保留"哪个 client、哪个用户"的明细 |

## 7. 结论

认证跳板方案下：**权限审批做一次（补齐到 pm-assist 应用），新应用接入零审批**——飞书侧的唯一动作是 pm-assist 安全设置里加过一个固定回调 URL（一次性）；此后每个新应用的接入成本 = `hub_config.json` 加一项注册（热加载免重启）+ 应用侧改 4 处代码。接入应用需要 pm-assist 已开通的新权限时，hub 侧零改动、存量 token 无需重新授权。
