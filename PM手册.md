# PM 助手操作手册

> 适用版本：v1.2.0  
> 面向：东软睿驰自动驾驶团队项目经理、项目成员

---

## 一、为什么有这个助手

项目管理的大量价值，在于**把散落在会议、消息、口头沟通里的信息，变成可追踪、可查询、能驱动行动的结构化记录**。

但这件事在实际工作中往往做不到——不是PM不知道该记什么，而是：

- 开完会还要花时间整理会议纪要、更新风险表
- 客户消息散落在飞书各个群里，没有一个地方汇总
- 里程碑和待办靠人脑记，出了问题才发现
- 新来的PM根本不知道项目背景，每次都要问老人

这个助手的目的，就是**把信息提取、结构化记录、风险追踪这些事自动化**，让PM的精力集中在判断和沟通上，而不是整理表格。

---

## 二、助手是怎么工作的

### 它有记忆

助手会记住你告诉它的事情——不是靠聊天历史，而是写入一个结构化的项目知识库。这个知识库包括：

- **预设背景**：公司/部门/项目的固定背景知识，每次对话自动带入（不用你反复解释"雅迪是什么项目"）
- **风险与问题**：你提到的风险、问题、阻塞项，会被提取并追踪状态
- **里程碑与计划**：项目关键节点，随时可查当前进展
- **待办事项**：可以关联到具体风险或里程碑，不是孤立的清单
- **决策记录**：重要决策有据可查，不靠脑子记

### 它主动提取信息

你不需要按格式填表。你只需要**正常和它说话**——比如把会议内容发给它、把客户消息转发给它——它会从自然语言里识别出风险、待办、里程碑等信息，给你一张确认卡片，你点"保存"就入库了。

### 它知道你在哪个项目

每个人绑定在一个项目下。和助手说话时，它知道你说的是哪个项目的事，不会把雅迪的问题混入其他项目。

---

## 三、开始使用

### 第一步：申请加入

在任意和助手的对话窗口发送：

```
/start
```

查看可用项目列表，然后发：

```
/join 雅迪 pm
```

（或 `/join 雅迪 member`，PM完整权限，member只能查看和对话）

管理员审批通过后会收到通知，即可开始使用。

### 怎么和助手说话

在飞书群或和助手的单聊中，**@机器人** 然后说任何话。助手会根据你的角色给出不同深度的回答——PM会看到更完整的项目上下文，成员看到的是简化版。

---

## 四、典型工作场景

### 场景1：开完会，怎么把信息变成记录

**不用写会议纪要**。直接把会议要点发给助手：

> @助手 今天和雅迪客户开了个对齐会，他们反馈说充电模块的标定结果和他们内部测试数据对不上，要求我们下周三给出原因分析报告。另外张总提到如果问题没解决，可能影响Q3的里程碑验收。

助手会从这段话里识别出：
- 一个**问题/风险**（充电模块标定数据不对齐，可能影响Q3验收）
- 一个**待办**（下周三出分析报告）

并弹出一张确认卡片，你确认保存，信息就进了追踪系统。

---

### 场景2：有个风险需要记录和跟踪

你知道有个问题需要认真追踪，直接命令：

```
/risk add issue high 充电模块标定数据与客户测试结果不一致 | 客户内部测试与我们标定结果有偏差，需定位根因，若未解决将影响Q3里程碑验收
```

格式是：`/risk add [类型] [优先级] [标题] | [描述]`

**类型**：
- `risk` — 潜在风险（还没发生但可能发生）
- `issue` — 已经在发生的问题
- `blocker` — 严重阻塞，项目推不下去了
- `dependency` — 依赖外部交付，你在等别人

**优先级**：`high` / `medium` / `low`

之后随时可以查风险列表：`/risk list`，或者看某条详情：`/risk show 12`（12是ID）。

风险解决了就关闭它：`/risk close 12`

---

### 场景3：围绕一个风险建行动计划

有了风险记录之后，建待办并关联到它：

```
/todo 联系标定组确认测试环境参数 risk 12
/todo 下周三前出充电模块标定偏差分析报告 risk 12
```

（12 是风险ID，从 `/risk list` 里看）

这样待办和风险12绑定，以后用 `/risk show 12` 看详情时会同时看到所有关联待办的状态，不用单独翻待办列表。

> AI自动分解风险为待办列表是管理员功能（`/admin fact decompose`），如果需要，可以请管理员帮你跑一次，结果用确认卡片交还给你操作。

---

### 场景4：管理日常待办

**快速建一条独立待办**：

```
/todo 周五前准备客户汇报PPT
```

**查看所有未完成待办**：

```
/todo list
```

**标记完成**：

```
/todo done 5
```

待办也可以挂到里程碑下，方便看某个阶段的所有任务：

```
/todo 准备Q3验收材料 plan 3
```

（3是里程碑ID，用 `/schedule list` 查）

---

### 场景5：查看里程碑进展

```
/schedule list
```

看到所有进行中的里程碑。点卡片上的"详情"，或者：

```
/schedule show 3
```

可以看到这个里程碑下的所有未完成待办、状态、截止日期。

---

### 场景6：快速记一条笔记/知识点

开会或和客户沟通时发现一些重要背景信息：

```
/note 雅迪方面的技术接口人是李工（电话13xxx），充电模块验收标准参考标准号GB/T 27930
```

这条信息会存入项目知识库，之后AI回答问题时会自动参考它。

---

### 场景7：生成项目状态汇报

需要给领导或客户汇报项目现状时：

```
/status run
```

助手会根据当前的风险、待办、里程碑生成一份结构化汇报，发到你的飞书。直接作为汇报底稿，按需调整即可。

---

### 场景8：AI 帮你整理项目数据（洗盘）

每周或定期运行一次"洗盘"，让AI扫描项目全量数据，告诉你：
- 哪些风险可以关闭了（状态过期）
- 哪些记录重复了可以合并
- 有没有遗漏的风险或行动项

```
/review run
```

助手会发一张报告卡片，列出建议操作，你点按钮确认执行，或者忽略。

系统每天早上9点也会自动发早报，包含前一天洗盘结果的摘要。

---

### 场景9：问助手项目相关的问题

助手不只是记录工具，也是项目顾问。直接问：

> @助手 现在雅迪项目最大的风险是什么，Q3验收有没有风险？

> @助手 充电模块相关的待办有哪些还没完成？

> @助手 我要给客户写邮件说明延期原因，帮我起草一下

助手会基于知识库里的实际数据回答，而不是泛泛而谈。

---

## 五、和AI对话的几个技巧

**1. 直接说，别想格式**

不需要用特定句式。把消息、会议记录、客户原话发给助手，它会处理。

**2. 信息越具体越好**

"有个问题"不如"充电模块标定结果和客户数据有5%偏差，客户要求下周三给答复"——后者助手能提取出截止日期、问题描述、责任方。

**3. 不确定的事也可以记**

不知道答案也没关系，记"待确认"比不记要好。风险和问题是被追踪的，不是被评判的。

**4. 助手不会主动"已记录"**

它不会说"好的，我帮你记录了"这种话——这是设计决定，避免虚假安全感。信息是否入库，以确认卡片操作为准。

---

## 六、常用命令速查

### 日常使用

| 场景 | 命令 |
|------|------|
| 查看并申请加入项目 | `/start` → `/join 雅迪 pm` |
| AI对话 | @助手 [任意内容] |
| 清除对话历史（开新话题） | `/clear` |

### 风险与问题

| 场景 | 命令 |
|------|------|
| 查看风险列表 | `/risk list` |
| 新建风险 | `/risk add [risk\|issue\|blocker\|dependency] [high\|medium\|low] 标题 \| 描述` |
| 查看详情 | `/risk show [ID]` |
| 关闭风险 | `/risk close [ID]` |

### 待办事项

| 场景 | 命令 |
|------|------|
| 查看待办 | `/todo list` |
| 新建待办 | `/todo [内容]` |
| 关联到风险 | `/todo [内容] risk [风险ID]` |
| 关联到里程碑 | `/todo [内容] plan [里程碑ID]` |
| 完成待办 | `/todo done [ID]` |
| 取消待办 | `/todo cancel [ID]` |

### 里程碑

| 场景 | 命令 |
|------|------|
| 查看里程碑 | `/schedule list` |
| 查看详情+关联待办 | `/schedule show [ID]` |

### 汇报与分析

| 场景 | 命令 |
|------|------|
| 生成项目状态汇报 | `/status run` |
| 手动触发洗盘 | `/review run` |
| 快速记笔记 | `/note [内容]` |

### 其他

| 场景 | 命令 |
|------|------|
| 查看当前版本 | `/version` |
| 查看帮助 | `/help` |
| 退出项目 | `/leave` |

---

## 七、权限说明

| 角色 | 能做什么 |
|------|---------|
| **PM** | 全部功能：AI对话、风险管理、待办、洗盘、汇报 |
| **成员** | 轻量AI对话、查看里程碑，不能建风险/待办 |
| **管理员** | 在PM权限基础上可管理用户、假设库、组织结构 |

申请时选 `pm` 还是 `member`，根据你实际需要参与的深度决定。一般项目负责人选 `pm`，关注进展的同事选 `member`。

---

*有问题或建议，联系杜莹芳。*
